import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'angularIonic',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
